{% extends 'base.html.twig' %}

{% block title %}Accueil | IMDb {% endblock %}

{% block body %}
<div class="col d-flex justify-content-center">
    <h1 class="text-white">Bienvenue sur</h1><span class="bg-warning border-warning rounded text-center fw-bold mx-2 " style="width: 100px ;">IMDb</span>
    
</div>
<div class="mt-5">
    {% for type, message in app.flashes %} 
            {# {{ dump(app.flashes, type, message) }}  #}
        <div class="col-6 mx-auto text-center alert alert-{{type}}">
                {{ message.0 }}
        </div>
    {% endfor %}
</div>
{% endblock %}
